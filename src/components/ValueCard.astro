---
import Icon from "@/components/BrandIcon.astro";

interface Props {
  icon: string;
  title: string;
  description: string;
  /** Number badge in the corner, for ordered lists */
  index?: number;
}

const { icon, title, description, index } = Astro.props;
---

<article
  class="group relative h-full rounded-sm border border-integra-gray-200 bg-integra-white p-7 hover:bg-integra-gray-100 hover:border-integra-red transition-colors duration-200"
>
  {index !== undefined && (
    <span
      aria-hidden="true"
      class="absolute -top-3 -right-3 inline-flex size-7 items-center justify-center rounded-sm border border-integra-gray-200 bg-integra-white text-xs font-mono font-semibold text-integra-gray-500 group-hover:border-integra-red/40 group-hover:text-integra-red-700 transition-colors duration-300"
    >
      {String(index).padStart(2, "0")}
    </span>
  )}
  <div
    class="inline-flex size-12 items-center justify-center rounded-sm bg-integra-gray-900 text-integra-white mb-5 group-hover:bg-integra-red transition-[transform,background-color] duration-300 ease-out"
  >
    <Icon name={icon} class="size-6" aria-hidden="true" />
  </div>
  <h3 class="text-xl font-semibold text-integra-gray-900 mb-2">
    {title}
  </h3>
  <p class="text-sm leading-relaxed text-integra-gray-700">
    {description}
  </p>
</article>
