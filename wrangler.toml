name = "integrautomacao-legacy-redirects"
main = "workers/legacy-redirects.ts"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

# Bind the Worker to a route on the integrautomacao.com.br zone.
# The route is registered via the Cloudflare dashboard or via wrangler deploy
# after the zone is added to the Cloudflare account.
#
# Production routes (uncomment after the zone is in Cloudflare):
# routes = [
#   { pattern = "integrautomacao.com.br/*", zone_name = "integrautomacao.com.br" }
# ]
