# Always include optional deps (cross-platform binaries like
# lightningcss-linux-x64-gnu, @emnapi/runtime). Without this, a lock
# generated on Windows can omit Linux binaries that Cloudflare Pages
# needs at install time, breaking `npm ci` in the build environment.
include=optional
